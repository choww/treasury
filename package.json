{
  "name": "treasury",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "transpile": "babel ./server --out-dir dist-server",
    "clean": "rimraf dist-server",
    "build": "npm run clean && npm run transpile",
    "start": "node ./dist-server/bin/www",
    "dev": "npm run build && node ./dist-server/bin/www"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "dependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.5",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv-safe": "^8.2.0",
    "express": "~4.16.1",
    "googleapis": "^39.2.0",
    "mongoose": "^5.9.16",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.3",
    "rimraf": "^3.0.2"
  }
}
